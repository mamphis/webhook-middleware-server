{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "attach",
            "name": "Debug: app-name",
            "remoteRoot": "/app",
            "localRoot": "${workspaceFolder}",
            "cwd": "${workspaceFolder}",
            "port": 9229,
            "restart": true,
            "address": "0.0.0.0"
        }
    ]
}
